#list don't need to push to github and track in github, like environment

.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
data/raw/
data/images

# Ignore raw data & generated files
data/images/
data/corpus/
data/embeddings_samples/
data/samples/
data/raw/
# Allow index_samples for deployment (small files, needed for Render free tier)
# data/index_samples/ - NOT ignored, needed for Render free tier

# Ignore virtual env
.venv/

# Ignore system files
.DS_Store.env
.env.*
replacements.txt
.env
.env.*
replacements.txt

# Personal documentation (not for public repo)
docs/WHY_FEATURES.md
docs/WHY_FASTAPI.md
docs/*.md

# Ignore data files, but allow index_samples for deployment
# Note: Can't use data/ and then un-ignore, so we're specific instead
data/corpus/*.parquet
data/embeddings_samples/*
data/images/*
data/samples/*
data/raw/*
# Allow index_samples (needed for Render free tier deployment)
# data/index_samples/ is NOT ignored
*.parquet
# Allow .npy and .faiss files in index_samples directory
data/**/*.npy
data/**/*.faiss
!data/index_samples/*.npy
!data/index_samples/*.faiss
__pycache__/
*.pyc